/** Small uppercase field label used above form controls in dialogs. */
export function SectionLabel({ children }: { children: React.ReactNode }) {
  return <div className="mb-2 text-11 font-bold uppercase leading-none tracking-5 text-fnt">{children}</div>;
}
